<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Reset Your Password - Traction</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f4f6fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
        }
        .email-container {
            max-width: 600px;
            margin: 40px auto;
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 18px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .header {
            background-color: #0052cc;
            padding: 24px;
            text-align: center;
        }
        .header h1 {
            color: #fff;
            font-weight: 700;
            margin: 0;
            font-size: 28px;
            letter-spacing: 1px;
        }
        .content {
            padding: 32px 40px;
        }
        .content h2 {
            margin-top: 0;
            font-size: 24px;
            color: #222;
        }
        .content p {
            font-size: 16px;
            line-height: 1.6;
            margin: 16px 0;
        }
        .btn {
            display: inline-block;
            background-color: #0052cc;
            color: #fff !important;
            padding: 14px 28px;
            margin-top: 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #003d99;
        }
        .footer {
            padding: 24px 40px;
            font-size: 13px;
            color: #999;
            text-align: center;
            border-top: 1px solid #eee;
        }
        @media (max-width: 640px) {
            .email-container {
                margin: 20px 16px;
            }
            .content {
                padding: 24px 16px;
            }
            .footer {
                padding: 16px;
            }
        }
    </style>
</head>
<body>
<div class="email-container">
    <div class="header">
        <h1>Traction</h1>
    </div>
    <div class="content">
        <h2>Hello, {{full_name}} 👋</h2>
        <p>We received a request to reset the password for your Boxbox account.</p>
        <p>If you made this request, please click the button below to change your password:</p>
        <a href="{{url}}" class="btn" target="_blank" rel="noopener">Reset Password</a>
        <p>If you did not request a password reset, please ignore this email. No changes have been made to your account.</p>
    </div>
    <div class="footer">
        Sincerely,<br />
        The Traction Support Team
    </div>
</div>
</body>
</html>
